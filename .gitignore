node_modules/
npm-debug.log*
.env
.env.*
.DS_Store
# 1) .gitignore に Obsidian を追加（スニペットのみ許可）
cat >> .gitignore <<'EOF'

# Obsidian app files (keep only snippets)
.obsidian/*
!.obsidian/snippets/
!.obsidian/snippets/news-table.css
EOF

# 2) 既にトラッキングされてる .obsidian を一旦外す（--cached なのでローカルは消えない）
git rm -r --cached .obsidian

# 3) 例外としてスニペットだけ再ステージ
git add .gitignore .obsidian/snippets/news-table.css

# 4) コミット & プッシュ
git commit -m "chore(repo): ignore Obsidian app files; keep snippet only"
git push
