# 2025-11-08

```dataview
TABLE WITHOUT ID
  default(title, file.name) AS タイトル,
  "[[" + file.path + "|記事ページへ]]" AS "記事ページへ",
  "[" + default(host, "元記事") + "](" + default(source_url, url) + ")" AS "引用元"
FROM "articles"
WHERE dateformat(date, "yyyy-MM-dd") = this.file.name
SORT file.name
`````

